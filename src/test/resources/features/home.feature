# Placeholder feature for the template.
# Replace this file (and add new .feature files) with the real scenarios for your project.
# Each feature typically has a theme (e.g. Login, Dashboard, Checkout) and scenarios in Gherkin.

@Template
Feature: Application placeholder

  # Single fake scenario so the test run passes. Replace with your real flows.
  Scenario: Application opens successfully
    Given I open the application
    Then the application page is displayed
